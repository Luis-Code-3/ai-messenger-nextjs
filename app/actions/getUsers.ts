
const getUsers = () => {
    //:P Must be logged in
    //:P Prevent sensitive Data from going out
    try {
        // Prisma Logic to get all Users
        // return users
    } catch (err) {
        console.log(err);
        return null
    }
};

export default getUsers;