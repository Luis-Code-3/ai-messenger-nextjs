
const getOneUser = () => {
    //:P Must be logged in
    //:P Prevent sensitive Data from going out
    try {
        // Prisma Logic to get one User
        // return user
    } catch (err) {
        console.log(err);
        return null
    }
};

export default getOneUser;